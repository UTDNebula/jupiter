/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['about.utdnebula.com', `github.com`, 'lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
